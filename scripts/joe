#!/usr/bin/env sh

#
# link config files
#

rm -rf ~/.joe/

ln -sf /bootstrap.sh/homefs/.joerc            ~/
ln -sf /bootstrap.sh/homefs/.joe/             ~/
ln -sf /bootstrap.sh/homefs/.selected_editor  ~/
