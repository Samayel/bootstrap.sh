 check host shell.johm.cst.fra1-ipv4 with address shell.johm.cst.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host ttrss.johm.cst.fra1-ipv4 with address ttrss.johm.cst.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host sandstorm.svc.fra1-ipv4 with address sandstorm.svc.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host tokbela.cst.fra1-ipv4 with address tokbela.cst.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host s2000.cst.fra1-ipv4 with address s2000.cst.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host klio.cst.fra1-ipv4 with address klio.cst.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host stats.fra1-ipv4 with address stats.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.nyc3-ipv4 with address vpn.nyc3.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.fra1-ipv4 with address vpn.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.ams3-ipv4 with address vpn.ams3.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.sgp1-ipv4 with address vpn.sgp1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.sfo2-ipv4 with address vpn.sfo2.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.tor1-ipv4 with address vpn.tor1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.lon1-ipv4 with address vpn.lon1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host vpn.blr1-ipv4 with address vpn.blr1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host dox.fra1-ipv4 with address dox.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host dox.ams3-ipv4 with address dox.ams3.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host ns.fra1-ipv4 with address ns.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host ca.fra1-ipv4 with address ca.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host mx.fra1-ipv4 with address mx.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles

 check host docker.fra1-ipv4 with address docker.fra1.do.quyo.net
   group network
   if failed ping4 for 3 times within 12 cycles then alert  # IPv4 only
   every 5 cycles
