 check process unbound with matching "/usr/sbin/unbound"
   group daemon
   group unbound
   start program = "/usr/sbin/service unbound start"
   stop  program = "/usr/sbin/service unbound stop"
   if 5 restarts within 5 cycles then timeout
